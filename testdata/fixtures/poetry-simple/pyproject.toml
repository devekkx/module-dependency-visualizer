[tool.poetry]
name = "poetry-app"
version = "1.0.0"
description = "A simple poetry app"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.31.0"

[tool.poetry.dev-dependencies]
pytest = "^7.4.0"
